{
    "$schema": "https://json.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build",
        "dev": "vite",
        "stripe-mcp": "npx @stripe/mcp --tools=all --api-key=${STRIPE_SECRET}",
        "setup-stripe-mcp": "./setup-stripe-mcp.sh"
    },
    "devDependencies": {
        "@stripe/mcp": "^0.2.4",
        "@tailwindcss/vite": "^4.1.13",
        "axios": "^1.11.0",
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^2.0.0",
        "tailwindcss": "^4.1.13",
        "vite": "^7.0.4"
    },
    "dependencies": {
        "playwright": "^1.55.0"
    }
}
